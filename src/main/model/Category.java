package model;

//This enum represents the different categories of items in a menu.
//Each category represents a specific type of dish.

public enum Category {
    APPETIZER,
    MAIN,
    DESSERT,
    DRINK,
}
